package build

import mill.*, scalalib.*

object counter extends ScalaModule {
  def scalaVersion = "2.13.16"

  def mvnDeps = Seq(
    mvn"org.chipsalliance::chisel:6.7.0"
  )
  
  def scalacPluginMvnDeps = Seq(
    mvn"org.chipsalliance:::chisel-plugin:6.7.0"
  )
}
